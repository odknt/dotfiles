background no
out_to_console yes
out_to_x no
update_interval 0.3
total_run_times 0
use_spacer none
 
TEXT
^p(;-1)^bg()  ^fg()${exec sh -c "ratpoison -c 'windows' | grep -e '[0-9]*\*' | awk 'END{gsub(/[0-9]*\*/,\"\"); print}' | grep -oe '^.\{1,100\}'"}   
